# Target backend untuk proxy development, tanpa trailing slash.
VITE_BACKEND_URL=http://localhost:8080

# Base path yang dipakai browser. Gunakan /api agar request melewati proxy.
VITE_API_BASE_URL=/api
